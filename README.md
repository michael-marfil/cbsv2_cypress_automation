# Project Name
cbsv2-cypress_automation

## Prerequisites

- Node.js ≥ 18.18 (20 or 22 recommended)
- npm ≥ 9

## Quick start (after git clone)

```bash
npm ci #or
npm install

cp .env.example .env
# ← then edit .env with your real credentials

npx cypress open        # interactive mode
# or
npm run test            # headless